import crypto from'node:crypto';
export default async function handler(req:any,res:any){if(req.method!=='GET')return res.status(405).json({error:'GET only'});const client=process.env.GOOGLE_DRIVE_CLIENT_ID,secret=process.env.OAUTH_STATE_SECRET;if(!client||!secret)return res.status(500).json({error:'Google Drive OAuth is not configured'});const origin=`https://${req.headers.host}`;const payload=Buffer.from(JSON.stringify({exp:Date.now()+10*60*1000,nonce:crypto.randomBytes(16).toString('hex')})).toString('base64url');const sig=crypto.createHmac('sha256',secret).update(payload).digest('base64url');const state=`${payload}.${sig}`;const q=new URLSearchParams({client_id:client,redirect_uri:`${origin}/api/drive-callback`,response_type:'code',scope:'https://www.googleapis.com/auth/drive.file',access_type:'offline',prompt:'consent',include_granted_scopes:'true',state});res.redirect(`https://accounts.google.com/o/oauth2/v2/auth?${q}`)}